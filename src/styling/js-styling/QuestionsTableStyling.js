import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    table: {
        minWidth: 650,
    },
    infoIcon: {
        color: "orange"
    },
    minusButton: {
        marginRight: "10px"
    },
    paper: {
        width: "90%",
        height: "200px",
        margin: "auto"
    }
});
